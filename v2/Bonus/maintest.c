/* #include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int main(int argc, char const *argv[])
{
	char array[10] = {0};

	read(0, array, 10);
	write(1, array, 10);
	read(0, array, 10);
	write(1, array, 10);
	return 0;
}
 */